/*#include <stdio.h>

typedef struct point {
	int x, y;
}a;

int equal(a p1, a p2);

int main(void) {
	a a1 = { 1,2 };
	a a2 = { 3,5 };

	int c = equal(a1, a2);

	if (c == 1) {
		printf("(%d, %d) == (%d, %d)", a1.x, a1.y, a2.x, a2.y);
	}
	else
		printf("(%d, %d) != (%d, %d)", a1.x, a1.y, a2.x, a2.y);

	return 0;
}

int equal(a p1, a p2) {
	if (p1.x == p2.x && p1.y == p2.y) {
		return 1;
	}
	else
		return 0;
}*/